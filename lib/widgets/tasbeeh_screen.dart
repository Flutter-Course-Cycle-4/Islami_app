import 'package:flutter/material.dart';

class TasbeehScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tasbeeh Screen'),
    );
  }
}
