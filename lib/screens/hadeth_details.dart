import 'package:flutter/material.dart';

class HadethDetails extends StatelessWidget {
  static const routeName = 'hadeth_details';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('hadeth details'),
      ),
    );
  }
}
